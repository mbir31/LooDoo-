rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow players (both guest and authenticated) to create and join rooms, update board state, send voice signals
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
